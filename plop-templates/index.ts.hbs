/**
 * Modulo: {{pascalCase name}}
 *
 * Exporta los componentes del modulo para registro en la aplicacion.
 *
 * @module modules/{{dashCase name}}
 */

export { {{camelCase name}}Routes } from './routes/{{dashCase name}}.routes.js';
export { {{camelCase name}}Service } from './services/{{dashCase name}}.service.js';
export type { {{pascalCase name}} } from './services/{{dashCase name}}.service.js';
